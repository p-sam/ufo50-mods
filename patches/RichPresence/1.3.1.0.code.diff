--- original/gml_GlobalScript_scrGetRecords.gml
+++ patched/gml_GlobalScript_scrGetRecords.gml
@@ -33,6 +33,10 @@
         {
             _records[i] = scrString("bar_no_record");
         }
+        with (obj_richpresence)
+        {
+            records[i] = _records[i];
+        }
         _records[i] = string_copy(_records[i], 1, 16);
     }
     return _records;
--- original/gml_GlobalScript_scrPlayGame.gml
+++ patched/gml_GlobalScript_scrPlayGame.gml
@@ -10,6 +10,10 @@
     {
         return false;
     }
+    with (obj_richpresence)
+    {
+        alarm[0] = 1;
+    }
     global.currGame = arg0;
     global.currGameID = global.mGameID[arg0];
     global.solutionState = false;
--- original/gml_GlobalScript_scrSetRecord.gml
+++ patched/gml_GlobalScript_scrSetRecord.gml
@@ -8,5 +8,10 @@
     {
         scrWriteStringManual(0, "preGoldRecord" + string(global.currGameID), arg1);
     }
+    var _gameWinVal = scrReadReal("gameWin" + string(global.currGameID), 0) >= global.GOLD_WIN;
+    with (obj_richpresence)
+    {
+        records[global.currGame] = scrGetRecord(global.currGameID, _gameWinVal);
+    }
     return true;
 }
